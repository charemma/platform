apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jobscout
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/charemma/jobscout
    targetRevision: HEAD
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: jobscout
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
